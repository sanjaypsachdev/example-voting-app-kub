kind: Service
apiVersion: v1
metadata:
  name:  db
  labels:
    name: db-service
    app: demo-voting-app
spec:
  type:  ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
  selectors:
    name: postgres-pod
    app: demo-voting-app